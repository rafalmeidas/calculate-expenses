function Dashboard() {
  // botão incluir
  // tabela listando
  // descrição, valor, forma de pagamento, parcelado ? número da parcela

  // quando o usuário ter oferta e dicimo marcada
  // tabela de dizímo e oferta

  return <div>Dashboard</div>;
}

export default Dashboard;
